# API Keys
OPENAI_API_KEY="sk-你的真实API_KEY"
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4o-mini"

# 可选：如果你上次已经创建好了一个角色，可以填在这里，否则留空
AGENT_ID=""

# 可选：PostgreSQL 连接字符串（留空则使用内存存储）
# Docker Compose 部署时自动注入，无需手动配置
DATABASE_URL=""
